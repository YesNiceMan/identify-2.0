# 资源标本馆

本页由 `scripts/make-samples.mjs` 生成，用于在无网络环境下验证扫描、识别与按类型导出的完整链路。

## 设计意图

- 文字、图片、视音频、文档、样式表、字体、压缩包都被引用了一次
- 相对路径与绝对路径混排，检验 URL 解析
- 同一资源被多处引用，检验去重与计数
- 懒加载属性与内联 JSON 中暗藏链接，检验深度提取

## 导出承诺

所有导出的文件都是原始字节：图片不会被压缩重编码，音视频不会被转码，文档不会被改写。

> 识别，然后原样带走。
